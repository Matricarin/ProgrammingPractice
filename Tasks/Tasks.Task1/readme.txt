*** Word Counter ***

The word counter accepts a filename as a command-line parameter, where the file contains the text to be analyzed. 
Upon reading and processing the file, the counter generates a CSV file that lists each word, 
its frequency, and its frequency as a percentage.
